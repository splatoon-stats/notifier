# Splatoon Stats Notifier

## Getting Started

### Using custom configuration

All available options are listed in [`.env.example`](.env.example).
Copy `.env.example` into `assets/.env`, then edit the copied file.
